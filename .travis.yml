language: node_js
cache: yarn
node_js:
  - "8"
jobs:
  include:
    - stage: lint
      script: yarn lint
    - stage: test
      script: yarn build
install:
  - npm install -g yarn
  - yarn --pure-lockfile
notifications:
  email:
    on_failure: always
    on_success: change
  irc:
    channels:
      - "chat.freenode.net#hearthsim-commits"
    use_notice: true
    skip_join: true
    on_failure: always
    on_success: change
    template:
      - "(%{branch} @ %{commit} : %{author}): %{message} %{build_url}"
